// SUPRESSION DE L'EMAIL DE LA LISTE
document.querySelector('#email-resume-tr-<%= @email.id %>').remove();

// SUPRESSION DE L'EMAIL DE L'AFFICHAGE
document.getElementById('current-email-subject').textContent = "";
document.getElementById('current-email-content').textContent = "";

// RAZ DE TOUS LES BOUTONS
btnGroup.querySelectorAll('*').forEach(n => n.remove());